---
title: Repository inventory
description: Structured inventory, ownership, dependencies, suspicious-content disposition, and baseline map.
---

# Repository inventory

Baseline inventory contained 562 tracked files (about 3.69 MB). The audited
candidate contains 633 versionable files (about 3.95 MiB): 319 under `skills/`,
172 under `docs/`, 83 under `specs/`, 44 skill packages, 5 modules, and 115
cataloged helper/test scripts. The 71-file increase is the documented audit,
foundation, role, tutorial, policy, configuration, and validation material—not
an unexplained generated tree.

| Path | Purpose and audience | Dependencies / authority | Audit disposition |
| --- | --- | --- | --- |
| `README.md`, `FAQ.md` | Root discovery and first use | Canonical public docs and stable install locator | Retained; corrected preview/version routing and terminology |
| `docs/` | MkDocs learning, reference, operations, adoption | `mkdocs.yml`, lock file, generated catalog | Retained; expanded and validated |
| `skills/<name>/` | Installable bounded capability | `SKILL.md`, scripts/references/tests, shared runtime | Retained; 44/44 inventoried |
| `skills/_shared/` | Canonical deterministic runtime and tests | Python 3.10+, mirror sync | Retained; all tests now discovered by CI |
| `skills/ai-sdlc-shared-runtime/` | Installable mirror of shared runtime | Generated from `_shared` | Retained; byte-sync gate required |
| `modules/` | Five module manifests | Harness API compatibility | Retained; stale core test corrected |
| `specs/`, `specs-refiniment/` | Implementation and refinement evidence | Markdown authority plus `_ai_sdlc` projections | Retained; spelling is a compatibility route, not renamed silently |
| `compatibility/` | Protected v1 names/flags/routes/release history | Compatibility validator | Retained; wording alias and bounded audit semantics added |
| `.github/workflows/` | Python matrix, docs build, candidate install, Pages | Full-SHA Actions, locked Python dependencies | Retained and hardened |
| `.github/CODEOWNERS`, `dependabot.yml` | Review ownership and dependency update proposals | GitHub settings must enforce | Added; files alone are not enforcement proof |
| `examples/` | Health-service and fixture workflows | Python standard library | Retained; used in baseline and regression tests |
| `concepts/`, `guides/` | Historical noncanonical notes | Public docs are authoritative | Retained as deprecated source notes; no canonical page may route users here |
| `requirements-docs.txt` | Direct documentation intent | MkDocs Material 9.7.7 | Retained |
| `requirements-docs.lock` | Resolved transitive graph with hashes | Generated by uv | Added; CI uses `--require-hashes` |
| `config/ai-sdlc-install-record.schema.json` | Portable install-record contract | Consumer install and update guides | Added and source-validated |
| `config/ai-sdlc-managed-skills.txt` | Sorted ownership inventory for upgrade/uninstall safety | Must exactly match installable skill directories | Added and source-validated |
| `SECURITY.md`, `SUPPORT.md` | Disclosure and support boundaries | Repository-owner settings | Added; no service-level agreement promised |
| `LICENSE` | Legal grant | Owner/legal decision | Missing external blocker; not fabricated |

## Cleanliness disposition

No tracked `.DS_Store`, `Thumbs.db`, swap, backup, log, cache, build, local
environment, archive, credential, private-key, broken-symlink, or machine-local
absolute path was confirmed. High-confidence token scans found no committed
secret. False positives were GitHub `id-token: write` and parser variable names.

Generated `site/`, `_site/`, Python caches, write locks, derived context, and
temporary agent directories are ignored. `.gitignore` now covers Python/test,
operating-system, editor, and local environment files while allowing safe
`.env.example`/`.env.sample` fixtures.

The similarly named shared runtime trees are intentional canonical/installable
mirrors and are guarded by `sync_installed_runtime.py --check`; they are not
independent duplicates. Historical `concepts/` and `guides/` remain a search
ambiguity risk and are accepted only while root/public navigation never points
to them.

## Technology and installation

- Python standard-library helpers and `unittest`; minimum documented Python
  3.10.
- MkDocs Material 9.7.7 site with a fully resolved hashed dependency lock.
- Node.js 22.20.0+ and pinned third-party Skills CLI 1.5.19 for installation.
- GitHub Actions on explicit Ubuntu 24.04 with Python 3.10/3.13.
- Project-scoped installation into `.agents/skills/` plus selected host paths.

The primary documentation entry point is `README.md`, followed by `docs/start.md`.
